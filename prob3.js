// Given and character if it is a consonant print "Consonant"

// declare the variable first
let char = 'i';

//if character is consonant, print it
if (char != 'a' && char != 'e' && char != 'i' && char!= 'o' && char != 'u'){
  console.log('Consonant');
  
}else {
  console.log('Vowel');
}